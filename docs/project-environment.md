# Project Environment
 Empty fields are #TODO for setup.

## Host (virtual)
* **Operating system:** Windows 11, current release.
* **Exact build number:** Run `winver` during first setup.
* **Build pin date:** Date the snapshot was taken and pinned.
* **Hypervisor:** VirtualBox (add version.)
* **VM snapshot identifier:** ASRE_Snapshot_1.0.0

## Host (bare metal)
* **Hardware:** CPU model. Get notes on allocator behavior, large-pages support, NUMA topology.
* **Operating system build:** Ditto.

## Tooling
Record installed versions. Setup scripts go in `src/`.
* **`[WINDBG]` version:**
* **Compiler toolchain:** Visual Studio edition, clang-cl, or mingw-w64, versions.
* **Sysinternals suite:** Snapshot date.
* **`[PROCHACKER]` version:**

## Python environment
* **Python version:**
* **Virtual environment location:**
* **Lockfile:** `pip freeze > requirements.txt` 